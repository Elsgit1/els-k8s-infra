apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: my-app-pdb
  namespace: default # namespace must match ns of application whose pods are being protected
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: my-app # label must match label of application whose pods are being protected
